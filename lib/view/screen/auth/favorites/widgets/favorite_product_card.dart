import 'package:flutter/material.dart';

class FavoriteProductCard extends StatelessWidget {
  FavoriteProductCard({@required this.idx});

  final int idx;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
